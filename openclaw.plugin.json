{
  "id": "workboard-controller",
  "name": "Workboard Controller",
  "description": "Reconciles Workboard cards, dispatches ready work, wakes owner sessions for terminal results, and notifies owners when cards start.",
  "version": "0.7.0",
  "activation": {
    "onStartup": true,
    "onConfigPaths": [
      "plugins.entries.workboard-controller"
    ]
  },
  "contracts": {
    "tools": [
      "workboard_controller_status",
      "workboard_controller_tick",
      "workboard_create_owned",
      "workboard_owner_bind"
    ],
    "gatewayMethodDispatch": [
      "authenticated-request"
    ]
  },
  "toolMetadata": {
    "workboard_controller_status": {
      "optional": true
    },
    "workboard_controller_tick": {
      "optional": true
    },
    "workboard_create_owned": {
      "optional": true
    },
    "workboard_owner_bind": {
      "optional": true
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Start the controller service on Gateway startup. Default true."
      },
      "boardId": {
        "type": "string",
        "description": "Workboard board id to watch. Default default."
      },
      "subscriptionId": {
        "type": "string",
        "description": "Existing Workboard notification subscription id to reuse."
      },
      "target": {
        "type": "string",
        "description": "Notification subscription target marker. Default workboard-controller."
      },
      "pollIntervalMs": {
        "type": "number",
        "description": "Notification poll interval in milliseconds. Default 15000."
      },
      "batchLimit": {
        "type": "number",
        "description": "Maximum Workboard notifications to process per tick. Default 50."
      },
      "dispatchCooldownMs": {
        "type": "number",
        "description": "Minimum delay between dispatch calls. Default 2000."
      },
      "reconcileIntervalMs": {
        "type": "number",
        "description": "Periodic Workboard dispatch/reconciliation interval in milliseconds. This lets Workboard reclaim expired claims and block timed-out running cards even when no terminal notification was emitted. Set 0 to disable. Default 60000."
      },
      "dispatchTimeoutMs": {
        "type": "number",
        "description": "Gateway self-route timeout for Workboard dispatch in milliseconds. Default 60000."
      },
      "gatewayBaseUrl": {
        "type": "string",
        "description": "Optional Gateway HTTP base URL for /tools/invoke and the controller self-route. Default http://127.0.0.1:${OPENCLAW_GATEWAY_PORT || gateway.port || 18789}."
      },
      "gatewayToolSessionKey": {
        "type": "string",
        "description": "Session key passed to /tools/invoke for Workboard notification tool policy routing. Default main."
      },
      "ownerRoutes": {
        "type": "array",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "sessionKey": {
              "type": "string",
              "description": "Full direct owner/routing session key. Preserve the channel-specific key exactly; do not use agent shortcuts."
            },
            "tenant": {
              "type": "string",
              "description": "Optional tenant match from public Workboard card metadata."
            },
            "boardId": {
              "type": "string",
              "description": "Optional Workboard board id match."
            },
            "agentId": {
              "type": "string",
              "description": "Optional Workboard card agent id match."
            }
          },
          "required": [
            "sessionKey"
          ]
        },
        "description": "Channel-agnostic owner routing table. Each entry must include sessionKey and at least one of tenant, boardId, or agentId. Match priority is tenant+boardId+agentId, then boardId+agentId, then tenant+boardId or tenant+agentId, then single dimensions; equal priority uses declaration order."
      },
      "startNotifyEnabled": {
        "type": "boolean",
        "description": "Notify the owner/routing session when dispatch starts Workboard cards. Default true."
      },
      "startNotifySessionKey": {
        "type": "string",
        "description": "Legacy global start-notification fallback used only when ownerRoutes has no match."
      },
      "terminalWakeEnabled": {
        "type": "boolean",
        "description": "Run an owner-session embedded-agent wake for terminal Workboard events: completed, failed, stale, dispatch blocked, and worker start failures. Default true; when absent, legacy wakeEnabled is honored."
      },
      "terminalWakeDebounceMs": {
        "type": "number",
        "minimum": 0,
        "maximum": 60000,
        "description": "Non-sliding terminal owner wake coalescing window in milliseconds, anchored at the first pending event for an idle owner. Default 1000."
      },
      "wakeEnabled": {
        "type": "boolean",
        "description": "Legacy alias for terminalWakeEnabled. Used only when terminalWakeEnabled is absent."
      },
      "wakeFallbackSessionKey": {
        "type": "string",
        "description": "Legacy global fallback retained for start notification compatibility only. Terminal owner wakes require ownerRoutes."
      },
      "wakeFallbackAgentId": {
        "type": "string",
        "description": "Fallback agent id when an event/card has no agent. Default main."
      },
      "wakeTimeoutMs": {
        "type": "number",
        "description": "Terminal wake/start notification embedded-agent timeout in milliseconds. Default 120000."
      },
      "wakeToolsAllow": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Optional tool allowlist for terminal wake runs."
      },
      "compatibleOpenClawVersions": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Exact OpenClaw versions accepted by this compatibility seam."
      },
      "allowUntestedOpenClawVersion": {
        "type": "boolean",
        "description": "Disable exact OpenClaw version fail-fast. Default false."
      },
      "archiveEnabled": {
        "type": "boolean",
        "description": "Archive eligible done Workboard cards. Default false."
      },
      "archiveDryRun": {
        "type": "boolean",
        "description": "Report archive candidates without mutating cards. Default true."
      },
      "archiveCompletedGraphAfterMs": {
        "type": "number",
        "description": "Cooling period for all-done linked components before archive. Default 86400000."
      },
      "archiveStandaloneAfterMs": {
        "type": "number",
        "description": "Cooling period for standalone done cards before archive. Default 604800000."
      },
      "archiveRequireProof": {
        "type": "boolean",
        "description": "Require every card to have non-failed proof before archive. Default true."
      },
      "archiveScanIntervalMs": {
        "type": "number",
        "description": "Minimum delay between archive scans. Default 3600000."
      }
    }
  }
}
